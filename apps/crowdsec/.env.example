# Bouncer API key for the Traefik plugin to authenticate against crowdsec's
# local API. Generate after crowdsec is up and running:
#   docker exec crowdsec cscli bouncers add traefik-bouncer
# then paste the printed key here (in the real .env, not this template).
CROWDSEC_BOUNCER_KEY=changeme
