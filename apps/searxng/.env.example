# Best practice is to copy this file to .env and change these values

# Overwrites the "ultrasecretkey" placeholder in settings.yml at runtime
# (searx/settings_defaults.py maps this env var straight onto
# server.secret_key). Generate with `openssl rand -hex 32`.
SEARXNG_SECRET=changeme
