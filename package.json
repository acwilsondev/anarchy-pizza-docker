{
  "private": true,
  "description": "Dev tooling for homelab-docker (markdown linting). Not a published package.",
  "scripts": {
    "lint:md": "markdownlint-cli2",
    "lint:md:fix": "markdownlint-cli2 --fix"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.23.2"
  }
}
