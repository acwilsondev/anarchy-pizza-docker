version: '3.9'
services:
  filebrowser:
    image: filebrowser/filebrowser
    container_name: filebrowser
    restart: unless-stopped
    volumes:
      - /storage/appdata/silver/filebrowser/.database.db:/.database.db
      - /storage:/srv
    networks:
      - anarchy-pizza

networks:
  anarchy-pizza:
    external:
      name: anarchy-pizza
