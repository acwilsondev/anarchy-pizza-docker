# Account created on first run (must match the LLDAP/Authelia username so
# the Remote-User header from Authelia's forward-auth maps to this account)
FRESHRSS_USERNAME=aaron
FRESHRSS_ADMIN_EMAIL=admin@example.com
FRESHRSS_ADMIN_PASSWORD=changeme
