services:
  synapse:
    image: matrixdotorg/synapse:latest
    container_name: synapse
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ${STORAGE_ROOT}/gold/synapse:/data
    depends_on:
      - synapse-postgresql
    networks:
      - private
      - homelab
    # Only publish if you are NOT using a reverse proxy.
    # If you use Traefik/Caddy/Nginx on homelab, you can omit ports entirely.
    # ports:
    #   - "8008:8008"

  synapse-postgresql:
    image: postgres:18
    container_name: synapse-postgresql
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ${STORAGE_ROOT}/gold/synapse-postgresql:/var/lib/postgresql
    networks:
      - private

networks:
  homelab:
    name: homelab
    external: true
  private:
    driver: bridge

