services:

  lldap:
    image: lldap/lldap:stable
    container_name: lldap
    restart: unless-stopped
    environment:
      - TZ=${TZ}
      - LLDAP_LDAP_BASE_DN=${LLDAP_BASE_DN}
      - LLDAP_LDAP_USER_EMAIL=${LLDAP_ADMIN_EMAIL}
      - LLDAP_LDAP_USER_PASS=${LLDAP_ADMIN_PASSWORD}
      - LLDAP_JWT_SECRET_FILE=/secrets/jwt_secret
      - LLDAP_KEY_SEED_FILE=/secrets/key_seed
      # Used only when manually running /app/bootstrap.sh (see migration_plan.md)
      - LLDAP_ADMIN_USERNAME=admin
      - LLDAP_ADMIN_PASSWORD=${LLDAP_ADMIN_PASSWORD}
      - USER_CONFIGS_DIR=/bootstrap/user-configs
      - GROUP_CONFIGS_DIR=/bootstrap/group-configs
    volumes:
      - ${STORAGE_ROOT}/silver/lldap/data:/data
      - ${STORAGE_ROOT}/silver/lldap/secrets:/secrets:ro
      - ${STORAGE_ROOT}/silver/lldap/bootstrap:/bootstrap:ro
    ports:
      # LDAP and the admin web UI are intentionally NOT routed through
      # Traefik/NPM. Bound to loopback only; exposed to the tailnet via
      # `tailscale serve --bg --tcp=17170 tcp://127.0.0.1:17170` instead
      # of a direct host-port bind to the Tailscale IP. Binding straight
      # to ${TAILSCALE_IP} raced tailscaled's own boot sequence and left
      # lldap permanently detached from the Docker network after a host
      # reboot - see incidents/2026-08-09-lldap-authelia-outage.md.
      - "127.0.0.1:17170:17170"
    deploy:
      resources:
        limits:
          memory: 256M
    networks:
      - homelab

networks:
  homelab:
    name: homelab
    external: true
